═══════════════════════════════════════════════════════════════
  🔍 快速验证命令（复制粘贴到SSH）
═══════════════════════════════════════════════════════════════

1. 连接服务器
───────────────────────────────────────────────────────────────
ssh root@107.173.154.147
# 密码：gJ75hNHdy90TA4qGo9


2. 验证磁盘空间（最重要！）
───────────────────────────────────────────────────────────────
df -h


期望结果：
  Filesystem      Size  Used Avail Use% Mounted on
  /dev/vda1        40G   32G   6G  85%  /

✅ 使用率 < 90% = 正常
❌ 使用率 > 90% = 需要进一步清理


3. 验证PM2状态
───────────────────────────────────────────────────────────────
pm2 status


期望结果：
  ┌─────┬────────────────┬──────┬────────┐
  │ id  │ name           │status│ restart│
  ├─────┼────────────────┼──────┼────────┤
  │ 0   │stock-tracker   │online│    0   │
  └─────┴────────────────┴──────┴────────┘

✅ status = online
❌ status = stopped/errored = 需要重启


4. 查看应用日志
───────────────────────────────────────────────────────────────
pm2 logs stock-tracker --lines 30 --nostream


查找关键信息：
  ✅ [代码规范化] 创业板: 3003300 → 300330
  ✅ [API] 成功获取数据
  ✅ ✓ Compiled successfully
  ❌ ERROR / Crash = 有问题


5. 验证Git仓库
───────────────────────────────────────────────────────────────
cd /www/wwwroot/stock-tracker || cd ~/stock-tracker
git log -1 --oneline


期望结果：
  b082167 fix: Fix stock code format issue

✅ 如果是这个提交 = 代码已更新
❌ 如果不是 = 需要重新部署


6. 测试API（可选）
───────────────────────────────────────────────────────────────
curl -s http://localhost:3000/api/stocks?date=2025-10-28 | head -20


✅ 返回JSON数据 = API正常
❌ 错误或无响应 = API有问题


═══════════════════════════════════════════════════════════════
  📊 一键验证所有项目
═══════════════════════════════════════════════════════════════

复制以下所有命令，一次性执行：

echo "=== 1. 磁盘空间 ==="
df -h | grep -E "Filesystem|/$|/www"
echo ""
echo "=== 2. PM2状态 ==="
pm2 status
echo ""
echo "=== 3. 最近日志 ==="
pm2 logs stock-tracker --lines 20 --nostream
echo ""
echo "=== 4. Git提交 ==="
cd /www/wwwroot/stock-tracker 2>/dev/null || cd ~/stock-tracker
git log -1 --oneline
echo ""
echo "=== 5. 进程检查 ==="
ps aux | grep node | grep -v grep | head -2
echo ""
echo "=== 验证完成 ==="


═══════════════════════════════════════════════════════════════
  🌐 浏览器验证
═══════════════════════════════════════════════════════════════

1. 访问网站：
   http://107.173.154.147:3000

2. 等待数据加载（可能需要10-30秒）

3. 点击"10-28"日期的"海南"板块

4. 查看"海峡创新"：
   ✅ 代码显示：(300330)  ← 6位数字，正确！
   ❌ 代码显示：(3003300) ← 7位数字，问题未修复

5. 查看涨跌幅数据是否合理


═══════════════════════════════════════════════════════════════
  🔧 如果验证失败的快速修复
═══════════════════════════════════════════════════════════════

问题1：磁盘仍然满（>90%）
───────────────────────────────────────────────────────────────
cd /www/wwwroot/stock-tracker || cd ~/stock-tracker
rm -rf node_modules .next
npm install
npm run build
pm2 restart stock-tracker


问题2：PM2显示stopped
───────────────────────────────────────────────────────────────
pm2 restart stock-tracker
pm2 logs stock-tracker --lines 50


问题3：代码未更新（不是b082167）
───────────────────────────────────────────────────────────────
cd /www/wwwroot/stock-tracker || cd ~/stock-tracker
git fetch origin
git reset --hard origin/main
npm install
npm run build
pm2 restart stock-tracker


问题4：网站无法访问
───────────────────────────────────────────────────────────────
pm2 restart stock-tracker
netstat -tuln | grep 3000
# 检查3000端口是否在监听


═══════════════════════════════════════════════════════════════
  ✅ 全部验证通过的标志
═══════════════════════════════════════════════════════════════

□ 磁盘使用率 < 90%
□ PM2 status = online
□ 无ERROR日志
□ Git提交 = b082167
□ 网站可以访问
□ 海峡创新代码 = 300330（6位）

如果以上全部打勾，恭喜！部署完全成功！🎉

═══════════════════════════════════════════════════════════════




