# ✅ Git拉取已成功！代码已更新到v4.3

# 服务器使用新版Docker命令（docker compose，无连字符）
# 请在SSH终端执行以下命令：

# 1. 停止现有容器
docker compose down

# 2. 重新构建镜像（包含新代码和新组件）
docker compose build --no-cache

# 3. 启动新容器
docker compose up -d

# 4. 等待服务启动
sleep 30

# 5. 检查容器状态
docker compose ps

# 6. 查看应用日志
docker compose logs --tail=50 stock-tracker

# 7. 测试访问
curl -I http://bk.yushuo.click

# ✅ 如果返回 HTTP/1.1 200 OK，部署成功！
# 🌐 浏览器访问 http://bk.yushuo.click 测试新功能