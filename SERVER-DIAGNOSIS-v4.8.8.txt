# 在服务器上执行此诊断命令（复制整个代码块）

cd /www/wwwroot/stock-tracker && echo "========================================" && echo "🔍 v4.8.8 部署诊断" && echo "========================================" && echo "" && echo "1️⃣ 检查Git版本:" && git log -1 --oneline && echo "" && echo "2️⃣ 检查关键代码修改:" && echo "   缓存时间 (应该是30分钟):" && grep "SEVEN_DAYS_CACHE_DURATION" src/app/api/stocks/route.ts | head -1 && echo "   Amount字段:" && grep "Amount?:" src/types/stock.ts | head -2 && echo "   成交额提取:" && grep "stockData\[6\]" src/app/api/stocks/route.ts | head -1 && echo "" && echo "3️⃣ Docker容器状态:" && docker ps | grep stock-tracker && echo "" && echo "4️⃣ 服务响应:" && curl -I http://localhost:3002 2>&1 | head -3 && echo "" && echo "5️⃣ 容器日志 (最近10行):" && docker logs --tail 10 stock-tracker-app 2>&1 && echo "" && echo "6️⃣ API数据测试:" && curl -s "http://localhost:3002/api/stocks?date=2025-10-09&mode=7days" | grep -o "sectorAmounts" | head -1 && echo "" && echo "========================================"
